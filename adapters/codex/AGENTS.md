# Design Language adapter for Codex

Before generating or editing user-facing language, read:

1. `rules/rules.yaml`
2. the relevant files in `spec/`
3. the appropriate prompt in `audit/prompts/` when auditing

Do not rewrite during an audit unless the user asks.

When editing, preserve meaning and report rule IDs for material changes.
